import React from "react";
import ReactDOM from "react-dom/client";
import SerieTP3 from "./SerieTP3";
import './app.css'
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <SerieTP3 />
  </React.StrictMode>
);
