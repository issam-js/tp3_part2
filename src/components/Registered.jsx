function Registered({ toggleRegistered }) {
  return (
    <div>
      <h1>Congrats! You are now registered!</h1>
      <button onClick={toggleRegistered}>back to inscription form?</button>
    </div>
  );
}

export default Registered;
